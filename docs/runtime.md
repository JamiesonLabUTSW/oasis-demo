# Frozen runtime details

[images.json](../images.json) lists five public packages: elephant-api,
elephant-migrate, maples-api, maples-worker, and maples-frontend (key `frontend`).

- Application: `d40f19552eabaec60746922fe0e1c96cea7e1556`
- Tag: `d40f1955-amd64-r1`
- Platform: `linux/amd64`

Use immutable `@sha256:` references from the manifest. Public copies preserve
original manifests/layers; labels may describe private canary provenance.
API and migration share a content digest. The separately supplied bootstrap
also needs public PostgreSQL, MinIO, MinIO client, and Prefect images.

On September 16, 2026, all five application packages were downloaded anonymously
and blob hashes verified; four infrastructure manifests resolved anonymously.
This proves artifact accessibility/integrity, not device installation or inference.

Optional MCP is excluded: its reference rubrics lacked established public-release
status. Do not enable optional profiles or republish it. The core image review
examined a synthetic `ExampleRubric.xlsx`; see [review scope](content-review-2026-09-16.md).

This repository supplies no model weights, provider keys, runtime credentials,
real notes, or private packets. Local login pages are generated by the installer.

The images are unsigned demonstration copies, not a production release or
validated assessment system. Preserve [LICENSE](../LICENSE) and
[THIRD_PARTY_NOTICES](../THIRD_PARTY_NOTICES); public access changes no terms.
