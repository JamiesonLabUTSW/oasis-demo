# Frozen runtime details

## Choose the matching platform

| Computer | Application | Platform / manifest | Publication |
| --- | --- | --- | --- |
| Windows Intel/AMD | d40f1955 | Linux AMD64 / [images.json](../images.json) | Public, anonymous downloads verified |
| Apple Silicon Mac | 4abb0bb2 | Linux ARM64 / [images-arm64.json](../images-arm64.json) | **Prepared, not published** |

Each selection has five application package references: elephant-api,
elephant-migrate, maples-api, maples-worker, and maples-frontend (key `frontend`).
API and migration share a content digest. Use immutable `@sha256:` references
from your kit's matching manifest. The separate version selections remain
independent; do not mix them or treat a platform label as recipient acceptance.

### Windows Intel/AMD

- Application: `d40f19552eabaec60746922fe0e1c96cea7e1556`
- Tag: `d40f1955-amd64-r1`
- Platform: `linux/amd64`

On September 16, 2026, all five application packages were downloaded anonymously
and blob hashes verified; four infrastructure manifests resolved anonymously.
This proves artifact accessibility/integrity, not device installation or inference.

### Apple Silicon Mac

- Application: `4abb0bb242228c528ef23876dd8746f3470882b3`
- Intended tag: `4abb0bb2-arm64-r1`
- Platform: `linux/arm64`
- Publication status: `PREPARED_NOT_PUBLISHED`

The prepared manifest selects exact native ARM64 runnable manifests, not a
combined multi-platform index. These are the intended public namespace copies;
their exact-content review, copy, and anonymous download/hash checks are pending.
No public ARM64 availability is claimed yet. The corresponding assisted Mac
package remains a separate handoff; no installer ZIP is published in this repo.
Fresh-recipient installation, login, sample visibility, and retained restart
remain open even after image publication.

## Content and installation boundary

Public copies preserve original manifests/layers; labels may describe private
build provenance. The separately supplied bootstrap also needs public
PostgreSQL, MinIO, MinIO client, and Prefect images. Those infrastructure images
are not copied into this namespace, and neither manifest is a complete installer.

Optional MCP is excluded: its reference rubrics lacked established public-release
status. Do not enable optional profiles or republish it. The AMD64 image review
examined a synthetic `ExampleRubric.xlsx`; see [review scope](content-review-2026-09-16.md).
The new ARM64 selection needs its own exact-content review before publication.

Only approved synthetic examples or separately verified public-domain demo
content may accompany the application. Institutional/proprietary rubrics,
private recordings, learner records/results, and restricted workshop packets
are excluded. Authorized restricted materials travel separately to named
recipients; receiving a pack does not authorize grading or provider processing.
See the [public content policy](public-content-policy.md).

This repository supplies no model weights, provider keys, runtime credentials,
real notes, or private packets. Local login pages are generated by the installer.

The images are unsigned demonstration copies, not a production release or
validated assessment system. Preserve [LICENSE](../LICENSE) and
[THIRD_PARTY_NOTICES](../THIRD_PARTY_NOTICES); public access changes no terms.
